<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.routepiresfront.viewmodel.DenunciaViewModel" />
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp"
        android:background="@android:color/white">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Cabeçalho -->
            <LinearLayout
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <ImageButton
                    android:id="@+id/button_back"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Voltar"
                    android:scaleType="centerInside"
                    android:src="@drawable/ic_voltar_custom" />

                <TextView
                    android:text="Denunciar"
                    android:textStyle="bold"
                    android:textSize="20sp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center" />
            </LinearLayout>

            <!-- EditText ligado ao motivo -->
            <EditText
                android:id="@+id/edtMotivoDenuncia"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/bg_edittext_selector"
                android:hint="Motivo da sua denúncia."
                android:minHeight="48dp"
                android:textSize="16sp"
                android:text="@={viewModel.motivo}" />

            <!-- EditText ligado à descrição -->
            <EditText
                android:id="@+id/edtDescricaooDenuncia"
                android:hint="Descrição da denúncia"
                android:background="@drawable/bg_edittext_selector"
                android:minHeight="120dp"
                android:gravity="top"
                android:layout_marginTop="8dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@={viewModel.descricao}" />

            <!-- Botão conectado ao método do ViewModel -->
            <Button
                android:id="@+id/btnEnviarDenuncia"
                android:text="Denunciar"
                android:textColor="@android:color/white"
                android:backgroundTint="@color/red"
                android:layout_marginTop="24dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{() -> viewModel.enviarDenuncia()}" />

        </LinearLayout>
    </ScrollView>

</layout>
